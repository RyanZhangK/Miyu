> 

# Spa Device

SpaDevice.getmanaged_object(self, id)

Binds `wp_spa_device_get_managed_object`

param self  
the spa device

param integer id  
the object id

returns  
the managed object or nil

SpaDevice.storemanaged_object(self, id, object)

Binds `wp_spa_device_store_managed_object`

param self  
the spa device

param integer id  
the object id

param GObject object  
a GObject to store or nil to remove the existing stored object
